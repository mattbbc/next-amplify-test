version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    test:
      commands:
        - npm test
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
